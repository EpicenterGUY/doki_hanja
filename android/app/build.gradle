plugins {
    id "com.android.application"
}

android {
    namespace "com.epicenterguy.dokihanja"
    compileSdk 35

    defaultConfig {
        applicationId "com.epicenterguy.dokihanja"
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName "0.1.0"
    }

    buildTypes {
        debug {
            applicationIdSuffix ".debug"
            versionNameSuffix "-debug"
        }
        release {
            minifyEnabled false
        }
    }
}

dependencies {
}
